services:
    # configuration par défaut pour les services de CE fichier
    _defaults:
        autowire: true
        autoconfigure: true

    # 🔹 Autowire toutes les classes du namespace App\
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # 🔹 Config spécifique à OutlookService (garde ce que tu as déjà)
    App\Service\OutlookService:
        arguments:
            $clientId: '%env(OAUTH_AZURE_CLIENT_ID)%'
            $clientSecret: '%env(OAUTH_AZURE_CLIENT_SECRET)%'
            $tenantId: '%env(MICROSOFT_TENANT_ID)%'
