{% extends 'base_public.html.twig' %}

{% block title %}Confirmation - Planifique{% endblock %}

{% block body %}
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="card border-0 shadow-lg p-5 text-center" style="max-width: 600px; border-radius: 20px;">

            {# Animation succès #}
            <div class="mb-4">
                <div class="d-inline-flex align-items-center justify-content-center rounded-circle bg-success bg-opacity-10 text-success" style="width: 80px; height: 80px;">
                    <i class="fas fa-check fa-3x"></i>
                </div>
            </div>

            <h1 class="h3 fw-bold text-dark mb-3">Rendez-vous confirmé !</h1>
            <p class="text-muted mb-4">
                Merci <strong>{{ rendezVous.prenom }}</strong>, votre demande a bien été enregistrée.<br>
                Un email de confirmation vient de vous être envoyé à <em>{{ rendezVous.email }}</em>.
            </p>

            {# Récapitulatif #}
            <div class="bg-light p-4 rounded-3 text-start mb-4 border">
                <div class="d-flex align-items-center mb-3">
                    <i class="far fa-calendar-alt text-primary me-3 fs-5" style="width: 25px;"></i>
                    <div>
                        <small class="text-uppercase text-muted fw-bold" style="font-size: 0.7rem;">Date & Heure</small>
                        <div class="fw-bold text-dark">{{ rendezVous.dateDebut|date('d/m/Y') }} à {{ rendezVous.dateDebut|date('H:i') }}</div>
                    </div>
                </div>

                <div class="d-flex align-items-center mb-3">
                    <i class="fas fa-user-tie text-primary me-3 fs-5" style="width: 25px;"></i>
                    <div>
                        <small class="text-uppercase text-muted fw-bold" style="font-size: 0.7rem;">Conseiller</small>
                        <div class="fw-bold text-dark">{{ rendezVous.conseiller.firstName }} {{ rendezVous.conseiller.lastName }}</div>
                    </div>
                </div>

                <div class="d-flex align-items-center">
                    <i class="fas fa-video text-primary me-3 fs-5" style="width: 25px;"></i>
                    <div>
                        <small class="text-uppercase text-muted fw-bold" style="font-size: 0.7rem;">Lieu</small>
                        <div class="fw-bold text-dark">{{ rendezVous.typeLieu }}</div>
                    </div>
                </div>
            </div>

            <div class="d-grid gap-2">
                {# BOUTON VERS LE SITE VITRINE PLANIFIQUE SA #}
                {# Pensez à remplacer l'URL par la vraie URL de votre site #}
                <a href="https://www.planifique.com/" class="btn btn-primary fw-bold py-3 rounded-pill shadow-sm">
                    Accéder au site PLANIFIQUE SA
                </a>
            </div>

        </div>
    </div>
{% endblock %}
