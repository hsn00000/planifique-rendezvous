{% extends 'base_public.html.twig' %}

{% block title %}Rendez-vous modifié - Planifique{% endblock %}

{% block body %}
    <div class="container d-flex align-items-center justify-content-center min-vh-100 py-5">
        <div class="card border-0 shadow-lg p-5 text-center" style="max-width: 600px; border-radius: 20px; width: 100%;">

            {# Logo #}
            <div class="mb-4">
                <img src="{{ asset('img/logo.png') }}" alt="Planifique" style="height: 50px; width: auto;">
            </div>

            {# Animation Succès #}
            <div class="mb-4">
                <div class="d-inline-flex align-items-center justify-content-center rounded-circle bg-success bg-opacity-10 text-success" style="width: 70px; height: 70px;">
                    <i class="fas fa-check fa-2x"></i>
                </div>
            </div>

            <h1 class="h3 fw-bold text-dark mb-2">Rendez-vous modifié !</h1>
            <p class="text-muted mb-4">
                Votre rendez-vous a été modifié avec succès.
                <br>
                Un email de confirmation vous a été envoyé à <strong>{{ rendezvous.email }}</strong>.
            </p>

            {# Ancienne date #}
            <div class="bg-light p-4 rounded-3 text-start mb-3 border">
                <div class="text-muted small text-uppercase fw-bold mb-2">Ancienne date</div>
                <div class="text-secondary">
                    {{ oldDateDebut|format_datetime(pattern="EEEE d MMMM yyyy", locale='fr')|capitalize }}
                    <br>
                    {{ oldDateDebut|date('H:i') }} - {{ oldDateFin|date('H:i') }}
                </div>
            </div>

            {# Nouvelle date #}
            <div class="bg-primary bg-opacity-10 p-4 rounded-3 text-start mb-4 border border-primary">
                <div class="text-primary small text-uppercase fw-bold mb-2">Nouvelle date</div>
                <div class="fw-bold text-dark">
                    {{ rendezvous.dateDebut|format_datetime(pattern="EEEE d MMMM yyyy", locale='fr')|capitalize }}
                    <br>
                    <span class="text-primary">{{ rendezvous.dateDebut|date('H:i') }} - {{ rendezvous.dateFin|date('H:i') }}</span>
                </div>
            </div>

            {# Récapitulatif #}
            <div class="bg-light p-4 rounded-3 text-start mb-4 border">
                <div class="d-flex align-items-center mb-3">
                    <i class="fas fa-user-circle text-primary me-3 fs-4" style="width: 30px; text-align: center;"></i>
                    <div>
                        <div class="text-uppercase text-muted fw-bold small" style="font-size: 0.7rem;">Conseiller</div>
                        <div class="fw-bold text-dark">{{ rendezvous.conseiller.firstName }} {{ rendezvous.conseiller.lastName }}</div>
                    </div>
                </div>
                <div class="d-flex align-items-center">
                    <i class="fas fa-stopwatch text-warning me-3 fs-4" style="width: 30px; text-align: center;"></i>
                    <div>
                        <div class="text-uppercase text-muted fw-bold small" style="font-size: 0.7rem;">Type</div>
                        <div class="fw-bold text-dark">{{ rendezvous.evenement.titre }}</div>
                    </div>
                </div>
            </div>

            {# Boutons #}
            <div class="d-grid gap-2">
                <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg fw-bold py-3 rounded-pill shadow-sm">
                    Retour à l'accueil
                </a>
                <a href="{{ path('app_booking_cancel', {'token': rendezvous.cancelToken}) }}" class="btn btn-outline-danger btn-lg fw-bold py-3 rounded-pill">
                    <i class="fas fa-times me-2"></i>Annuler ce rendez-vous
                </a>
            </div>

        </div>
    </div>
{% endblock %}
