<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Planifique{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📅</text></svg>">

    {# Bootstrap 5 & FontAwesome #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {# Ton fichier CSS personnalisé (AssetMapper) #}
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
        <script src="{{ asset('app.js') }}" defer></script>
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">

<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
        {# --- LOGO --- #}
        <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home') }}">
            {# Si le logo existe, on l'affiche, sinon on met une icône #}
            <img src="{{ asset('img/logo.png') }}" alt="Planifique" height="40" class="d-inline-block align-text-top"
                 onerror="this.style.display='none'; this.nextElementSibling.style.display='block';">

            {# Fallback si l'image ne charge pas #}
            <span style="display:none;"><i class="fas fa-calendar-check text-primary"></i> Planifique</span>
        </a>

        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">
                            Tableau de bord
                        </a>
                    </li>
                    {# Ajoute tes autres liens ici #}
                {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto align-items-center">
                {% if app.user %}
                    {# --- MENU UTILISATEUR --- #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown">
                                    <span class="text-end d-none d-lg-block">
                                        <small class="d-block text-muted" style="line-height:1; font-size:0.75rem;">Bonjour,</small>
                                        <span class="fw-bold">{{ app.user.firstName|default('Conseiller') }}</span>
                                    </span>
                            <div class="user-avatar">
                                {{ app.user.userIdentifier|first|upper }}
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg mt-2">
                            <li class="px-3 py-2">
                                <span class="small text-muted">Connecté en tant que</span><br>
                                <strong>{{ app.user.userIdentifier }}</strong>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="fas fa-power-off me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary rounded-pill px-4" href="{{ path('app_login') }}">
                            Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<main class="flex-grow-1">
    {# Messages Flash #}
    {% for label, messages in app.flashes %}
        <div class="container mt-4">
            {% for message in messages %}
                <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show bg-white" role="alert">
                    {% if label == 'success' %}<i class="fas fa-check-circle me-2 text-success"></i>{% endif %}
                    {% if label == 'error' %}<i class="fas fa-exclamation-circle me-2 text-danger"></i>{% endif %}
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        </div>
    {% endfor %}

    {% block body %}{% endblock %}
</main>

<footer class="py-4 mt-auto bg-white border-top">
    <div class="container text-center text-muted small">
        <p class="mb-0">&copy; {{ 'now'|date('Y') }} Planifique Rendez-vous. Solution pour conseillers.</p>
    </div>
</footer>

</body>
</html>
