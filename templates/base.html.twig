<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Planifique{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📅</text></svg>">

    {# --- CSS BOOTSTRAP 5 (CDN) --- #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    {% block stylesheets %}
        {# Si vous avez des styles personnalisés via AssetMapper, décommentez ceci : #}
        {# {{ importmap('app') }} #}
    {% endblock %}

    {% block javascripts %}
        {# --- JS BOOTSTRAP 5 (CDN) Indispensable pour le menu déroulant --- #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
        {# {{ importmap('app') }} #}
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100 bg-light">

{# --- DEBUT BARRE DE NAVIGATION --- #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        {# Logo / Marque #}
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="{{ path('app_home') }}">
            <i class="fas fa-calendar-check text-primary"></i>
            Planifique
        </a>

        {# Bouton Burger (Mobile) #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        {# Contenu du menu #}
        <div class="collapse navbar-collapse" id="navbarMain">

            {# Liens à gauche (Tableau de bord, etc.) #}
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_home') }}">
                            <i class="fas fa-home me-1"></i> Tableau de bord
                        </a>
                    </li>
                {% endif %}
            </ul>

            {# Liens à droite (Utilisateur / Connexion) #}
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

                {% if app.user %}
                    {# --- UTILISATEUR CONNECTÉ --- #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center fw-bold" style="width: 32px; height: 32px; font-size: 0.9rem;">
                                {{ app.user.userIdentifier|first|upper }}
                            </div>
                            <span>{{ app.user.firstName|default(app.user.userIdentifier) }}</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow">
                            <li>
                                <h6 class="dropdown-header">
                                    Connecté en tant que<br>
                                    <strong class="text-primary">{{ app.user.userIdentifier }}</strong>
                                </h6>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt me-2"></i> Se déconnecter
                                </a>
                            </li>
                        </ul>
                    </li>

                {% else %}
                    {# --- VISITEUR (NON CONNECTÉ) --- #}
                    <li class="nav-item">
                        <a class="btn btn-primary btn-sm px-4 rounded-pill" href="{{ path('app_login') }}">
                            <i class="fas fa-user me-1"></i> Se connecter
                        </a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>
{# --- FIN BARRE DE NAVIGATION --- #}

{# --- CONTENU PRINCIPAL --- #}
<main class="flex-grow-1">
    {# Affichage des messages Flash (Succès / Erreur) #}
    {% for label, messages in app.flashes %}
        <div class="container mt-3">
            {% for message in messages %}
                <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show shadow-sm" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        </div>
    {% endfor %}

    {% block body %}{% endblock %}
</main>

{# --- PIED DE PAGE --- #}
<footer class="py-4 bg-white mt-auto border-top">
    <div class="container text-center text-muted small">
        <p class="mb-1">&copy; {{ 'now'|date('Y') }} Planifique Rendez-Vous. Tous droits réservés.</p>
        <p class="mb-0">
            <a href="#" class="text-decoration-none text-muted me-2">Mentions légales</a>
            <a href="#" class="text-decoration-none text-muted">Confidentialité</a>
        </p>
    </div>
</footer>

</body>
</html>
